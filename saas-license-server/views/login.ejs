<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acesso Restrito - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #0d1117;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }

        .login-card {
            background-color: #161b22;
            padding: 40px;
            border-radius: 6px;
            width: 100%;
            max-width: 380px;
            border: 1px solid #30363d;
            box-shadow: 0 8px 24px rgba(0,0,0,0.5);
        }

        .brand-logo {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .brand-icon {
            font-size: 2.5rem;
            color: #F0B90B;
            margin-bottom: 15px;
        }

        .brand-title {
            color: #c9d1d9;
            font-size: 1.2rem;
            font-weight: 500;
        }

        .form-control {
            background-color: #0d1117 !important;
            border: 1px solid #30363d;
            color: #c9d1d9 !important;
            height: 45px;
            border-radius: 6px;
            padding-left: 40px;
            font-size: 14px;
        }

        .form-control:focus {
            border-color: #58a6ff;
            box-shadow: 0 0 0 3px rgba(88, 166, 255, 0.1);
        }

        .form-control::placeholder {
            color: #8b949e;
        }

        .input-icon {
            position: absolute;
            left: 14px;
            top: 14px;
            color: #8b949e;
            font-size: 14px;
            z-index: 4;
        }

        .btn-login {
            background-color: #F0B90B;
            border: 1px solid rgba(240, 185, 11, 0.5);
            color: #000;
            font-weight: 600;
            padding: 10px;
            width: 100%;
            margin-top: 10px;
            border-radius: 6px;
            font-size: 14px;
            transition: all 0.2s;
        }

        .btn-login:hover {
            background-color: #e5b00a;
            transform: none;
            box-shadow: none;
        }

        .alert {
            background: rgba(248, 81, 73, 0.1);
            border: 1px solid rgba(248, 81, 73, 0.4);
            color: #ff7b72;
            padding: 10px;
            font-size: 13px;
            border-radius: 6px;
        }

        /* Ajuste do Autofill do Chrome para não estragar a cor */
        input:-webkit-autofill,
        input:-webkit-autofill:hover, 
        input:-webkit-autofill:focus, 
        input:-webkit-autofill:active{
            -webkit-box-shadow: 0 0 0 30px #0d1117 inset !important;
            -webkit-text-fill-color: #c9d1d9 !important;
            transition: background-color 5000s ease-in-out 0s;
        }
    </style>
</head>
<body>

    <div class="login-card">
        <div class="brand-logo">
            <div class="brand-icon">
                <i class="fa-solid fa-lock"></i>
            </div>
            <div class="brand-title">Acesso Administrativo</div>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger text-center mb-4">
                <i class="fas fa-exclamation-circle me-2"></i><%= error %>
            </div>
        <% } %>

        <form action="<%= typeof actionUrl !== 'undefined' ? actionUrl : '/acesso-admin' %>" method="POST">
            <div class="position-relative mb-4">
                <input type="text" name="username" class="form-control ps-5" placeholder="Usuário" required autofocus>
                <i class="fas fa-user input-icon"></i>
            </div>
            
            <div class="position-relative mb-4">
                <input type="password" name="password" class="form-control ps-5" placeholder="Senha" required>
                <i class="fas fa-lock input-icon"></i>
            </div>
            
            <button type="submit" class="btn btn-login">
                Entrar
            </button>
        </form>
    </div>

</body>
</html>
