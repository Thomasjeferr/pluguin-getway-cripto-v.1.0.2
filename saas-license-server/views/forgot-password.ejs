<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Esqueci minha Senha - Plugin Cripto</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #0d1117;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }

        .login-card {
            background-color: #161b22;
            padding: 40px;
            border-radius: 6px;
            width: 100%;
            max-width: 420px;
            border: 1px solid #30363d;
            box-shadow: 0 8px 24px rgba(0,0,0,0.5);
        }

        .brand-logo {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .brand-icon {
            font-size: 2.5rem;
            color: #F0B90B;
            margin-bottom: 15px;
        }

        .brand-title {
            color: #c9d1d9;
            font-size: 1.2rem;
            font-weight: 500;
        }

        .form-control {
            background-color: #0d1117 !important;
            border: 1px solid #30363d;
            color: #c9d1d9 !important;
            height: 45px;
            border-radius: 6px;
            padding-left: 40px;
            font-size: 14px;
        }

        .form-control:focus {
            border-color: #58a6ff;
            box-shadow: 0 0 0 3px rgba(88, 166, 255, 0.1);
        }

        .form-control::placeholder {
            color: #8b949e;
        }

        .input-icon {
            position: absolute;
            left: 14px;
            top: 14px;
            color: #8b949e;
            font-size: 14px;
            z-index: 4;
        }

        .btn-submit {
            background-color: #F0B90B;
            border: 1px solid rgba(240, 185, 11, 0.5);
            color: #000;
            font-weight: 600;
            padding: 10px;
            width: 100%;
            margin-top: 10px;
            border-radius: 6px;
            font-size: 14px;
            transition: all 0.2s;
        }

        .btn-submit:hover {
            background-color: #e5b00a;
            transform: none;
            box-shadow: none;
        }

        .btn-link {
            background: transparent;
            border: none;
            color: #58a6ff;
            text-decoration: none;
            font-size: 13px;
            padding: 0;
            margin-top: 15px;
            display: block;
            text-align: center;
        }

        .btn-link:hover {
            color: #79c0ff;
            text-decoration: underline;
        }

        .alert {
            background: rgba(248, 81, 73, 0.1);
            border: 1px solid rgba(248, 81, 73, 0.4);
            color: #ff7b72;
            padding: 10px;
            font-size: 13px;
            border-radius: 6px;
            margin-bottom: 20px;
        }

        .alert-success {
            background: rgba(35, 134, 54, 0.1);
            border-color: rgba(35, 134, 54, 0.4);
            color: #3fb950;
        }

        .info-text {
            color: #8b949e;
            font-size: 13px;
            text-align: center;
            margin-top: 15px;
        }
    </style>
</head>
<body>

    <div class="login-card">
        <div class="brand-logo">
            <div class="brand-icon">
                <i class="fa-solid fa-key"></i>
            </div>
            <div class="brand-title">Recuperar Senha</div>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert">
                <i class="fas fa-exclamation-circle me-2"></i><%= error %>
            </div>
        <% } %>

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success">
                <i class="fas fa-check-circle me-2"></i><%= success %>
            </div>
        <% } %>

        <% if (typeof success === 'undefined' || !success) { %>
            <p class="info-text">Digite seu email e enviaremos um link para redefinir sua senha.</p>

            <form action="/esqueci-senha" method="POST">
                <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <% } %>
                <div class="position-relative mb-4">
                    <input type="email" name="email" class="form-control ps-5" placeholder="Seu email" required autofocus>
                    <i class="fas fa-envelope input-icon"></i>
                </div>
                
                <button type="submit" class="btn btn-submit">
                    <i class="fas fa-paper-plane me-2"></i>Enviar Link de Recuperação
                </button>
            </form>
        <% } %>

        <a href="/acesso-admin" class="btn-link">
            <i class="fas fa-arrow-left me-1"></i>Voltar para o login
        </a>
    </div>

</body>
</html>
